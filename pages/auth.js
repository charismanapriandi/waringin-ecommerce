import { FormLogin } from "../components"

const auth = ()=> {
    return (
        <div className="h-screen w-full flex justify-center items-center">
            <FormLogin></FormLogin>
        </div>
    )
}

export default auth